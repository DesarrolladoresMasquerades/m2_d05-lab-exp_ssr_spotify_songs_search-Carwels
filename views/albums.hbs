<h1> Albums </h1>

<section>

  {{#each album}}

    <div>
      <img src="{{images.0.url}}" alt="">

      <h2>{{name}}</h2>

      <button><a href="/tracks/{{id}}"> View All Tracks </a></button>
    </div>

  {{/each}}

</section>